# Native-app Payment Handlers

TODO: Brief overview

## Building the specification

This specification is written using
[bikeshed](https://speced.github.io/bikeshed). To build an HTML file from the
source `spec.bs`, install bikeshed and then run `bikeshed`. This will create a
`spec.html` file.
